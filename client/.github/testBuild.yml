name: testBuild

on:
  push:
    branches:
      - devOps

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    -uses: actions/checkout@v4
    -uses: actions/setup-node@v3
      with:
        node-version: '18'
    -run: npm install --force
    -run: npm run build
    
